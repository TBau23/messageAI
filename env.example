# Firebase Configuration
# Get these values from your Firebase project settings
# Firebase Console: https://console.firebase.google.com/

# Copy this file to .env and fill in your Firebase credentials

EXPO_PUBLIC_FIREBASE_API_KEY=your_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
EXPO_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Emulator Configuration (for development only)
# For testing on physical devices, set this to your computer's local IP address
# Leave commented out (or use 127.0.0.1) for iOS Simulator / Android Emulator
# To find your local IP:
#   - macOS: ifconfig | grep "inet " | grep -v 127.0.0.1
#   - Linux: ip addr show | grep "inet " | grep -v 127.0.0.1
#   - Windows: ipconfig | findstr IPv4
# Example: EXPO_PUBLIC_EMULATOR_HOST=192.168.1.100
# EXPO_PUBLIC_EMULATOR_HOST=127.0.0.1

# Note: All environment variables must use the EXPO_PUBLIC_ prefix
# to be accessible in the React Native app with process.env

# Usage:
# 1. Copy this file: cp env.example .env
# 2. Replace the placeholder values with your actual Firebase credentials
# 3. Never commit your .env file to version control

